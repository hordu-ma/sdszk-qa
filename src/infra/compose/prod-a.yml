services: {}

# 说明：A 服务器（172.18.6.117）已有线上 Nginx 在运行，本项目不独占 Nginx。
# 因此本文件不再用于启动 Nginx 容器，仅作为生产入口层部署占位文件保留。
# A 服务器请按以下方式部署：
# 1) 上传前端 dist 到现网 Nginx 约定静态目录
# 2) 将 ./nginx/nginx.conf 中的增量片段并入现有 Nginx 配置
# 3) 执行 nginx -t && nginx -s reload
